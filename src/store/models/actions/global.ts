export interface IDataLoadingState {
    type: Boolean;
    loading: boolean;
  }
export interface ISetCreateNewWalletActivitySpinner {
    createNewWalletActivitySpinner: boolean;
  }

export interface ISetQiWalletScanInProgress {
    qiWalletScanInProgress: boolean;
  }
export interface ISetQiWalletSyncInProgress {
    qiWalletSyncInProgress: boolean;
  }

